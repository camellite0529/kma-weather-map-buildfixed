# KMA Weather Map

지면용 날씨 레이아웃(Vite, TypeScript). 소스는 `frontend/`입니다.

## 실행

```bash
cd frontend
npm install
npm run dev
```

브라우저에서 기상청·미세먼지·천문 API 키를 입력합니다. 배포는 `npm run build` 후 `frontend/dist`를 정적 호스팅하면 됩니다.

지도 배경은 `frontend/public/map-bg.png`에 두면 됩니다.
